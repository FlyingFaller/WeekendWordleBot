[project]
name = "wordlebot"
version = "0.1.0"
description = "Basic wordle bot based on entropy optimization."
readme = "README.md"
requires-python = ">=3.13.5"
dependencies = [
    "ipykernel>=6.29.5",
    "numpy>=2.3.0",
    "requests>=2.32.4",
    "tqdm>=4.67.1",
]
